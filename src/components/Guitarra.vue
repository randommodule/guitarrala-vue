<script  setup>
//esto es un componente
//se define un macro para definir los props
//defineProps no requieren importarse, basicamente son metodos static
//props es de padre a hijo
//emit del hijo al padre
//se utilizar como contador de cantidad, para usarse en un inline handler
//se utiliza para consas sencillas como cambiar algo de color 
//para acciones complejas se usan los methond handlers, como validación de datos
const props=defineProps({
    guitarra: {
        type: Object,
        requered: true
    }
})
//esta variable es igual a un array function
defineEmits('agragregar-carrito')
</script>

<template>
    <div class="col-md-6 col-lg-4 my-4 row align-items-center">
                <div class="col-4">
                    <img class="img-fluid" v-bind:src="'/img/'+guitarra.imagen+'.jpg' " v-bind:alt="'imagen guitarra'+guitarra.nombre">
                </div>
                <div class="col-8">
                    <h3 class="text-black fs-4 fw-bold text-uppercase">{{ guitarra.nombre}}</h3>
                    <p>{{ guitarra.descripcion }}</p>
                    <p class="fw-black text-primary fs-3">${{guitarra.precio}}</p>
                    <button 
                        type="button"
                        class="btn btn-dark w-100 "
                        v-on:click="$emit('agregar-carrito',guitarra)"
                    >Agregar al Carrito</button>
                
                </div><!-- FIN GUITARRA -->
            </div>
</template>



<style lang="scss" scoped>

</style>